##
## Broadcast Machine 2 URI rewriting config.
##

##
## For when mod_rewrite is dynamically loaded.
##
<IfModule mod_rewrite.so>
	  RewriteEngine On
	  RewriteBase /bmachine2/
	  RewriteCond %{REQUEST_FILENAME} !-f
	  RewriteCond %{REQUEST_FILENAME} !-d
	  RewriteRule . /bmachine2/index.php [L]
</IfModule>

##
## For when mod_rewrite is compiled in. (Ubuntu default!)
##
<IfModule mod_rewrite.c>
          RewriteEngine On
          RewriteBase /bmachine2/
          RewriteCond %{REQUEST_FILENAME} !-f
          RewriteCond %{REQUEST_FILENAME} !-d
          RewriteRule . /bmachine2/index.php [L]
</IfModule>

# libphp5, what about mod_php, statically/dynamically compiled?
<IfModule libphp5.so>
	  # Disable magic quotes.
	  php_flag magic_quotes_gpc Off
</IfModule>