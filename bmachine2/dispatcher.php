<?php

// Grab the parameters
$param_1 = $_GET[1];
$param_2 = $_GET[2];
$param_3 = $_GET[3];

// If the first parameter is a tag, delegate to the tag controller.

// Instantiate singleton classes.


?>